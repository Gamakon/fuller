target/
*.so
*.dylib
*.dll
__pycache__/
*.egg-info/
build/
dist/
.venv/
.DS_Store

# agent worktrees (transient)
.claude/worktrees/

# nucleotable subsumed DB artifacts
*.duckdb
nucleotable.db
vendor-egglog/target/
hof_pickles/
logs/latest.jsonl

# LaTeX build artifacts. Scoped to docs/paper/ ON PURPOSE: a bare `*.log` would
# also swallow the 52 run logs under logs/, which are the record of the fits.
docs/paper/**/*.aux
docs/paper/**/*.log
docs/paper/**/*.out
docs/paper/**/*.synctex.gz

# A/B binary snapshots: 7.5 MB apiece, and rebuildable from the commit the
# run card names. The card is the record, not the binary.
logs/*/evolve_fit_*
# TSR sweep datasets: copied from hff/notebooks/sr_logs, not ours to version.
logs/tsr/data/
